import EmptyIcon from 'con-con/icons/EmptyIcon';
import { Center } from 'native-base';

const ImageFallback = () => (
  <Center flex={1}>
    <EmptyIcon color="text.400" size="100px" />
  </Center>
);

export default ImageFallback;
